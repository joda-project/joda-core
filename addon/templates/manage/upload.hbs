<section class="section">
  <div class="container">
    <h3>Upload resources</h3>
    <p>Choose a resource type you want to upload and then browse for files. Files can be in PDF, JPEG or PNG format, up to 300 MiB in total.</p>

    <label class="label">Create resources after upload</label>
    <p class="control">
      {{#radio-button value=null groupValue=resourceType class="radio" }}
        Do not create
      {{/radio-button}}
      {{#each joda.features as |feature|}}
        {{#radio-button value=feature.id groupValue=resourceType class="radio" }}
          {{feature.title}}
        {{/radio-button}}
      {{/each}}
    </p>

    <label class="label">Files</label>
    <p class="control">{{file-upload action="uploadCompleted" errorMessage=errorMessage progress=progress resourceType=resourceType }}</p>
    <p>&nbsp;</p>

    {{#if errorMessage}}
      <progress class="progress is-danger" value="{{progress}}" max="100">{{progress}}%</progress>
    {{else}}
      <progress class="progress is-primary" value="{{progress}}" max="100">{{progress}}%</progress>
    {{/if}}

    {{#if errorMessage}}
      <div class="notification is-danger">
        {{errorMessage}}
      </div>
    {{/if}}
  </div>
</section>
