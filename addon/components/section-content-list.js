import Component from '@ember/component';
import layout from '../templates/components/section-content-list';

export default Component.extend({
  layout,
  classNames: ['section'],
  tagName: 'section'
});
